name: Php cs test

on:
  push:
    branches: [ "feature/dev","feature/dev-1" ]


jobs:
  static-tests:
    
    name: Php cs test
    uses: ./.github/workflows/phpcs-test.yml
    with:
        code_directory: "app/"









